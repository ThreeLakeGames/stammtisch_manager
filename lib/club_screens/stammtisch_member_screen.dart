import 'package:flutter/material.dart';

class StammtischMemberScreen extends StatelessWidget {
  const StammtischMemberScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Member Screen"),
    );
  }
}
